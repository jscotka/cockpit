[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cockpit_testlib"
version = "0.0.2"
authors = [
    #TODO add authors
]
description = "Testlibrary of cockpit project"
requires-python = ">=3.7"
keywords = ["keywords"]
license = {text = "GPL3"}
classifiers = [
    #TODO add classifiers
]
dependencies = [
    # some deps
]

[project.optional-dependencies]
dev = [
    #TODO add optional dependencies
]

[project.gui-scripts]
foo = "foo:launch"

[tool.setuptools.package-dir]
common = "common"
"common.testlib" = "common"
"common.machine" = "machine"

[tool.setuptools.package-data]
common = ["*"]